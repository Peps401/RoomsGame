using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstCharVisibility : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject mainChar;
    void Start()
    {
        gameObject.SetActive(true);
    }

}
